CREATE DATABASE SWITCHBOARD;

USE SWITCHBOARD;

CREATE TABLE switchboard_details(id int,switchboard_name varchar(10),switchboard_type varchar(10),switchboard_use varchar(5),
switchboard_price int,switchboard_no bigint);

USE SWITCHBOARD;

DESC switchboard_details;